const db = require('../settings/db');

class Call {
    // constructor(entryId) {
    //     this.entryId = entryId;
    // }

    add(entryId) {
        // const sql = `INSERT calls()`
        // db.query()
    }

    static getAll() {

    }

    /**
     *
     * @param id personal key (pk)
     */
    static get(id) {

    }
}
